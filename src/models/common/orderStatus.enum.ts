enum OrderStatusEnum {
  CREATED = 'created',
  ON_THE_WAY = 'on-the-way',
  CANCELLED = 'cancelled',
  DELIVERED = 'delivered',
  RETURN_PLANCED = 'return-placed',
  RETURN_COMPLETE = 'return-completed',
}

export default OrderStatusEnum;
