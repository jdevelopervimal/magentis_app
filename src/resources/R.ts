import {colors, darkTheme} from './colors/colors';
import fonts from './fonts/fonts';
import {
  bodyTextStyle,
  fontSize,
  FontSizeEnum,
  FontWeightEnum,
  generateFontStyle,
  headerTextStyle,
  letterSpacing,
  lineHeight,
  subheaderTextStyle,
} from './fonts/fontStyles';
import AppImages from './images';
import {borderRadius, LayoutSizeEnum} from './layouts/layoutStyles';

const R = {
  colors,
  darkTheme,
  fonts,
  FontSizeEnum,
  FontWeightEnum,
  generateFontStyle,
  fontSize,
  letterSpacing,
  lineHeight,
  headerTextStyle,
  subheaderTextStyle,
  bodyTextStyle,
  LayoutSizeEnum,
  borderRadius,
  AppImages,
};

export default R;
